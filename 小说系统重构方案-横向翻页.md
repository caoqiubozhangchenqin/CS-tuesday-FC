# 小说系统重构方案 - 横向翻页模式

## 📋 重构目标
- ❌ 放弃章节解析（避免解析失败、超时等问题）
- ✅ 改为横向翻页阅读
- ✅ 只记录用户当前页码
- ✅ 简化系统架构，提升稳定性

---

## 🎯 核心变化对比

| 特性 | 旧方案（章节解析） | 新方案（横向翻页） |
|------|------------------|------------------|
| **解析方式** | 按章节解析并存储 | 不解析，直接读取原文 |
| **翻页方式** | 垂直滚动 | 横向翻页（左右滑动） |
| **进度记录** | 章节索引 + scrollTop | 当前页码 |
| **数据库** | novel_chapters 集合 | 不需要章节集合 |
| **云函数** | parseNovel 云函数 | 可能不需要 |
| **加载速度** | 需等待解析 | 即时阅读 |
| **存储成本** | 高（存储所有章节） | 低（只存储元数据） |

---

## ⚠️ 关键技术要点

### 1. 文本分页算法

#### 问题：如何将完整小说文本分成"页"？

**方案 A：前端动态分页**（推荐）
```javascript
// 优点：灵活，适配不同屏幕
// 缺点：切换设备时页码不同步

// 基于容器高度计算
const pageHeight = wx.getSystemInfoSync().windowHeight;
const lineHeight = 50; // rpx
const linesPerPage = Math.floor(pageHeight / lineHeight);
const charsPerLine = 25;
const charsPerPage = linesPerPage * charsPerLine;

// 分页逻辑
function splitToPages(fullText) {
  const pages = [];
  for (let i = 0; i < fullText.length; i += charsPerPage) {
    pages.push(fullText.substr(i, charsPerPage));
  }
  return pages;
}
```

**方案 B：服务端固定分页**
```javascript
// 优点：跨设备页码一致
// 缺点：不同屏幕显示效果不同

// 固定每页字数（如 500 字）
const CHARS_PER_PAGE = 500;

function getPage(fullText, pageNumber) {
  const start = pageNumber * CHARS_PER_PAGE;
  return fullText.substr(start, CHARS_PER_PAGE);
}
```

**推荐：混合方案**
- 服务端固定分页（保证跨设备一致性）
- 前端根据屏幕自适应显示（字体大小、行距调整）

---

### 2. 文件存储方式

#### 问题：TXT 文件如何存储？

**方案 A：云存储（推荐）**
```javascript
// 上传到云存储
wx.cloud.uploadFile({
  cloudPath: `novels/${bookId}.txt`,
  filePath: tempFilePath
});

// 阅读时按需下载
wx.cloud.downloadFile({
  fileID: 'cloud://xxx/novels/123.txt',
  success: res => {
    // 读取文件内容
    const fs = wx.getFileSystemManager();
    const content = fs.readFileSync(res.tempFilePath, 'utf8');
    // 分页显示
  }
});
```

**方案 B：云数据库（不推荐）**
```javascript
// 缺点：单文档最大 16MB，大文件需分片
db.collection('novels').add({
  data: {
    bookId: '123',
    content: fullText, // ⚠️ 可能超出限制
    totalPages: 1000
  }
});
```

**推荐：云存储 + 数据库元数据**
```javascript
// 数据库只存元信息
db.collection('novels').add({
  data: {
    bookId: '123',
    title: '小说名',
    author: '作者',
    fileID: 'cloud://xxx/novels/123.txt', // 云存储地址
    fileSize: 1024000, // 文件大小（字节）
    totalChars: 500000, // 总字数
    totalPages: 1000, // 总页数
    charsPerPage: 500 // 每页字数
  }
});
```

---

### 3. 阅读进度记录

#### 数据结构设计

**reading_progress 集合**：
```javascript
{
  _id: "auto-id",
  _openid: "user-openid", // 自动生成
  novelId: "book-123",
  currentPage: 58, // 当前页码（核心字段）
  totalPages: 1000,
  progress: 5.8, // 阅读进度百分比
  updateTime: 1700654321000
}
```

**保存进度**：
```javascript
async saveProgress(pageNumber) {
  const db = wx.cloud.database();
  const progressData = {
    novelId: this.data.bookId,
    currentPage: pageNumber,
    totalPages: this.data.totalPages,
    progress: (pageNumber / this.data.totalPages * 100).toFixed(2),
    updateTime: Date.now()
  };

  // 查询是否已有记录
  const exist = await db.collection('reading_progress')
    .where({ novelId: this.data.bookId })
    .get();

  if (exist.data.length > 0) {
    // 更新
    await db.collection('reading_progress')
      .doc(exist.data[0]._id)
      .update({ data: progressData });
  } else {
    // 新增（_openid 自动添加）
    await db.collection('reading_progress')
      .add({ data: progressData });
  }
}
```

---

### 4. 横向翻页实现

#### 方案 A：使用 swiper 组件（推荐）

**WXML**：
```xml
<swiper 
  class="reader-swiper"
  current="{{currentPage}}"
  bindchange="onPageChange"
  vertical="{{false}}"
  circular="{{false}}"
  indicator-dots="{{false}}">
  
  <swiper-item wx:for="{{visiblePages}}" wx:key="index">
    <scroll-view scroll-y class="page-content">
      <text>{{item.content}}</text>
    </scroll-view>
  </swiper-item>
</swiper>

<!-- 页码指示器 -->
<view class="page-indicator">
  {{currentPage + 1}} / {{totalPages}}
</view>
```

**JS（懒加载优化）**：
```javascript
data: {
  currentPage: 0,
  totalPages: 1000,
  visiblePages: [], // 只加载前后各1页（共3页）
  allContent: '' // 完整文本
},

onLoad() {
  // 加载小说
  this.loadNovel();
  // 恢复阅读进度
  this.loadProgress();
},

// 预加载相邻页
loadVisiblePages(centerPage) {
  const pages = [];
  for (let i = centerPage - 1; i <= centerPage + 1; i++) {
    if (i >= 0 && i < this.data.totalPages) {
      pages.push({
        index: i,
        content: this.getPageContent(i)
      });
    }
  }
  this.setData({ visiblePages: pages });
},

// 获取指定页内容
getPageContent(pageNum) {
  const charsPerPage = this.data.charsPerPage;
  const start = pageNum * charsPerPage;
  return this.data.allContent.substr(start, charsPerPage);
},

// 翻页回调
onPageChange(e) {
  const newPage = e.detail.current;
  this.setData({ currentPage: newPage });
  
  // 预加载新的相邻页
  this.loadVisiblePages(newPage);
  
  // 保存进度（防抖）
  clearTimeout(this.saveTimer);
  this.saveTimer = setTimeout(() => {
    this.saveProgress(newPage);
  }, 1000);
}
```

#### 方案 B：使用 movable-view（更灵活）

**WXML**：
```xml
<movable-area class="reader-area">
  <movable-view 
    direction="horizontal"
    x="{{-currentPage * pageWidth}}"
    bindchange="onMove">
    
    <view class="page" wx:for="{{pages}}" wx:key="index">
      {{item.content}}
    </view>
  </movable-view>
</movable-area>
```

---

### 5. 性能优化策略

#### 问题：大文件（如 10MB TXT）如何高效加载？

**策略 1：首次只下载部分内容**
```javascript
// 云函数：按页范围返回
exports.main = async (event) => {
  const { bookId, startPage, endPage } = event;
  const charsPerPage = 500;
  
  // 从云存储读取文件
  const fileContent = await readFile(bookId);
  
  // 只返回指定页范围
  const start = startPage * charsPerPage;
  const end = endPage * charsPerPage;
  
  return {
    content: fileContent.substr(start, end - start),
    totalChars: fileContent.length,
    totalPages: Math.ceil(fileContent.length / charsPerPage)
  };
};
```

**策略 2：分段缓存**
```javascript
// 缓存策略：每 100 页为一个分段
const SEGMENT_SIZE = 100; // 页
const segmentIndex = Math.floor(currentPage / SEGMENT_SIZE);

// 检查缓存
const cacheKey = `novel_${bookId}_seg_${segmentIndex}`;
let segmentContent = wx.getStorageSync(cacheKey);

if (!segmentContent) {
  // 下载该分段
  segmentContent = await loadSegment(bookId, segmentIndex);
  wx.setStorageSync(cacheKey, segmentContent);
}
```

**策略 3：虚拟列表（只渲染可见页）**
```javascript
// 始终只保持 3 个 swiper-item（前中后）
// 动态更新内容，而不是创建 1000 个 swiper-item
```

---

### 6. 跨设备同步问题

#### 问题：不同设备屏幕不同，页码不同步？

**解决方案：使用字符偏移量**

```javascript
// 不只记录页码，同时记录字符偏移量
{
  novelId: "123",
  currentPage: 58, // 当前设备的页码
  charOffset: 29000, // 字符偏移量（关键！）
  charsPerPage: 500, // 当前设备每页字数
  updateTime: 1700654321000
}

// 切换设备时，根据 charOffset 重新计算页码
function calculatePage(charOffset, newCharsPerPage) {
  return Math.floor(charOffset / newCharsPerPage);
}
```

**更好的方案：固定分页**
```javascript
// 服务端统一：每页 500 字（不受屏幕影响）
// 前端根据屏幕调整字体大小和行距适配
const STANDARD_CHARS_PER_PAGE = 500; // 固定
```

---

### 7. 数据库结构调整

#### 删除/弃用的集合
- ❌ `novel_chapters` - 不再需要章节存储

#### 保留的集合
- ✅ `novels` - 小说元信息
  ```javascript
  {
    _id: "book-123",
    _openid: "user-openid",
    title: "小说名",
    author: "作者",
    fileID: "cloud://xxx/novels/123.txt",
    coverUrl: "封面URL",
    totalChars: 500000,
    totalPages: 1000,
    charsPerPage: 500,
    uploadTime: 1700654321000
  }
  ```

- ✅ `reading_progress` - 阅读进度
  ```javascript
  {
    _id: "progress-id",
    _openid: "user-openid",
    novelId: "book-123",
    currentPage: 58,
    charOffset: 29000, // 新增：字符偏移量
    totalPages: 1000,
    progress: 5.8,
    updateTime: 1700654321000
  }
  ```

---

### 8. 云函数调整

#### 需要的云函数

**1. uploadNovel（上传小说）**
```javascript
// 功能：
// - 接收 TXT 文件
// - 上传到云存储
// - 计算总字数、总页数
// - 保存元信息到数据库

exports.main = async (event) => {
  const { fileID, title, author } = event;
  
  // 下载文件（临时）
  const res = await cloud.downloadFile({ fileID });
  const content = fs.readFileSync(res.tempFilePath, 'utf8');
  
  // 计算统计信息
  const totalChars = content.length;
  const charsPerPage = 500;
  const totalPages = Math.ceil(totalChars / charsPerPage);
  
  // 保存到数据库
  await db.collection('novels').add({
    data: {
      title,
      author,
      fileID,
      totalChars,
      totalPages,
      charsPerPage,
      uploadTime: Date.now()
    }
  });
  
  return { success: true, totalPages };
};
```

**2. getNovelContent（获取内容）**
```javascript
// 功能：按页范围返回内容（可选，也可前端直接下载）

exports.main = async (event) => {
  const { novelId, startPage, pageCount } = event;
  
  // 查询小说信息
  const novel = await db.collection('novels').doc(novelId).get();
  const { fileID, charsPerPage } = novel.data;
  
  // 下载文件
  const res = await cloud.downloadFile({ fileID });
  const content = fs.readFileSync(res.tempFilePath, 'utf8');
  
  // 提取指定页
  const start = startPage * charsPerPage;
  const end = start + pageCount * charsPerPage;
  
  return {
    content: content.substring(start, end),
    totalPages: Math.ceil(content.length / charsPerPage)
  };
};
```

#### 可以删除的云函数
- ❌ `parseNovel` - 不再需要解析

---

### 9. 用户体验优化

#### 翻页动画
```css
/* 平滑翻页效果 */
.reader-swiper {
  transition: transform 0.3s ease;
}

/* 翻页时添加翻书效果 */
.page-turning {
  animation: pageFlip 0.3s ease;
}

@keyframes pageFlip {
  0% { transform: rotateY(0deg); }
  50% { transform: rotateY(-10deg); }
  100% { transform: rotateY(0deg); }
}
```

#### 预加载提示
```javascript
// 首次加载时显示进度
wx.showLoading({
  title: '正在加载小说...',
  mask: true
});

// 加载完成后显示提示
wx.showToast({
  title: `共 ${totalPages} 页`,
  icon: 'success'
});
```

#### 快速跳转
```xml
<!-- 添加进度条 -->
<slider 
  value="{{currentPage}}"
  max="{{totalPages - 1}}"
  bindchange="onSliderChange"
  show-value/>
```

---

### 10. 测试关键点

#### 功能测试
- [ ] 上传不同大小的 TXT 文件（100KB, 1MB, 10MB）
- [ ] 横向翻页流畅度测试
- [ ] 阅读进度保存/恢复
- [ ] 跨设备同步（不同手机、不同屏幕尺寸）
- [ ] 快速跳转到指定页

#### 性能测试
- [ ] 大文件（10MB）加载时间 < 3 秒
- [ ] 翻页响应时间 < 100ms
- [ ] 内存占用 < 50MB
- [ ] 离线缓存功能

#### 边界测试
- [ ] 第一页/最后一页边界处理
- [ ] 空文件处理
- [ ] 特殊字符（emoji、繁体字）显示
- [ ] 网络中断时的处理

---

## 🚀 迁移步骤

### 阶段 1：数据准备（1 天）
1. 备份现有数据
2. 创建新的数据库集合结构
3. 编写数据迁移脚本（如果需要保留旧数据）

### 阶段 2：核心功能开发（2-3 天）
1. 实现横向翻页 UI
2. 实现文件上传和存储
3. 实现分页算法
4. 实现进度保存/加载

### 阶段 3：优化（1-2 天）
1. 性能优化（懒加载、缓存）
2. UI/UX 优化（动画、提示）
3. 跨设备同步优化

### 阶段 4：测试和发布（1 天）
1. 完整测试
2. 清理旧代码和云函数
3. 部署上线

**总计：5-7 天**

---

## 📝 注意事项总结

### ✅ 优势
- 避免章节解析失败问题
- 系统架构更简单
- 加载速度更快
- 存储成本更低
- 维护成本降低

### ⚠️ 挑战
- 需要处理不同屏幕尺寸的分页一致性
- 大文件加载需要优化策略
- 用户习惯变化（横向翻页 vs 垂直滚动）

### 🎯 关键决策
1. **分页方式**：建议使用固定字符数分页（500字/页）
2. **存储方式**：云存储 + 数据库元数据
3. **UI 组件**：swiper 组件（成熟稳定）
4. **进度同步**：记录字符偏移量 + 页码
5. **性能优化**：分段缓存 + 懒加载

---

## 📚 参考资源
- [微信小程序 swiper 组件文档](https://developers.weixin.qq.com/miniprogram/dev/component/swiper.html)
- [云存储文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/guide/storage.html)
- [性能优化指南](https://developers.weixin.qq.com/miniprogram/dev/framework/performance/)

---

**文档创建时间**: 2025-01-23  
**预计开发周期**: 5-7 天  
**建议优先级**: 高（解决核心痛点）
